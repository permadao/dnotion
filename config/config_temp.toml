appname = "dnotion"

# notion db
[notiondb]
    base_url = "https://api.notion.com"
	version = "2022-06-28"
    # secret for get redirect info
	client_secret = "<your client secret>" 
    client_id = "<your client id>"
    # dbs
    db_secret = "<notion db secret>" # secret for update dbs
    # contributors db
    db_contributors = "<notion db id>"
    # task db
    task_dbs = [
        "<uuid>", # content
        "<uuid>", # translation
        "<uuid>", # submission
        "<uuid>", # promotion
        "<uuid>", # activity
        "<uuid>", # admin
        "<uuid>"  # dev
    ]
    # workload db
    workload_dbs = [
        "<uuid>", # content
        "<uuid>", # translation
        "<uuid>", # submission
        "<uuid>", # promotion
        "<uuid>", # activity
        "<uuid>", # admin
        "<uuid>", # dev
        "<uuid>", # psp market
        "<uuid>" # psp prod
    ]
    # finance db
    finance_dbs = [
        "<uuid>", # content
        "<uuid>", # translation
        "<uuid>", # submission
        "<uuid>", # promotion
        "<uuid>", # activity
        "<uuid>", # admin
        "<uuid>", # dev
        "<uuid>", # psp market
        "<uuid>"  # psp prod
    ]

# everpay
[everpay]
	eth_prvkey = "<priv key>"
	everpay_url = "https://api.everpay.io"
    scan_url = "https://scan.everpay.io"
    app_name = "permadao-payroll"
    token_tagar = "arweave,ethereum-ar-AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA,0x4fadc7a98f2dc96510e42dd1a74141eeae0c1543"
# onboard

[log]
    save_file = false
    file_name = "dnotion.log"

# service
[service]
	port = 3002